# Quicker CLI Actions 参数参考

---

## 0208 操作Excel的某个区域或单元格

**类型:** Enum (子操作类型)

| Type | 说明 |
|------|------|
| `writeRange` | 写入区域数据 |
| `writeValue` | 写入单个单元格值 |
| `readRange` | 读取区域数据 |
| `readValue` | 读取单个单元格值 |
| `copy` | 复制区域 |
| `clear` | 清除区域内容 |

---

## 0704 运行脚本

**类型:** Enum

| Type | 说明 |
|------|------|
| `ps` | PowerShell 脚本 |
| `bat` | 批处理脚本 |
| `py` | Python 脚本 |

| 参数 | 说明 |
|------|------|
| `encoding` | 输入编码，默认 `utf-8` |
| `outputEncoding` | 输出编码，默认 `utf-8` |

---

## 1110 文件和目录操作

**类型:** Enum

| Type | 说明 |
|------|------|
| `list` | 列出目录内容 |
| `create` | 创建目录 |
| `delete` | 删除文件/目录 |
| `move` | 移动/重命名 |
| `copy` | 复制文件 |
| `exist` | 检查是否存在 |

---

## 0303 生成一个新的Guid

**类型:** Enum (输出格式)

| Format | 说明 |
|--------|------|
| `N` | 连续无分隔符 (default) |
| `D` | 连字符分隔 |
| `B` | 带花括号 |
| `P` | 带圆括号 |

---

## 1107 从文件路径中提取文件名文件夹等信息

**类型:** Enum

| Operation | 说明 |
|-----------|------|
| `name` | 文件名（含扩展名） |
| `nameNoExt` | 文件名（不含扩展名） |
| `ext` | 扩展名 |
| `dir` | 所在目录 |
| `fullDir` | 完整目录路径 |
| `drive` | 盘符 |

---

## 0905 调用第三方服务翻译文字到指定的语言

**类型:** Enum

| Operation | 说明 |
|-----------|------|
| `translate` | 执行翻译 |

| 参数 | 说明 |
|------|------|
| `vendor` | 翻译服务商 (如 `baidu`, `google`) |
| `vendorList` | 可用服务商列表 |

---

## 0414 调用Quicker的某个功能

**类型:** Enum

| Type | 说明 |
|------|------|
| `runAction` | 运行动作 |
| `sendEvent` | 发送事件 |

---

## 0415 获取资源管理器的当前文件夹路径

**类型:** Enum

| Operation | 说明 |
|-----------|------|
| `getPath` | 获取当前路径 |